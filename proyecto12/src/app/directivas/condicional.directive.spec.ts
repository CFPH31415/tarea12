import { CondicionalDirective } from './condicional.directive';

describe('CondicionalDirective', () => {
  it('should create an instance', () => {
    const directive = new CondicionalDirective();
    expect(directive).toBeTruthy();
  });
});
